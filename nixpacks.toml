[phases.setup]
nixPkgs = ["python312", "ffmpeg", "python3Packages.pip", "python3Packages.gunicorn"]

[phases.install]
commands = [
  "pip install -r requirements.txt"
]

[phases.build]
commands = []

[phases.start]
cmd = "gunicorn --bind 0.0.0.0:$PORT ytmp3_server:app"

[metadata]
name = "ytmp3-server"
